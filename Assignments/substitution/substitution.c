// Substitution : encrypt messages using a substitution cipher

#include <stdio.h>
#include <cs50.h>


int main(int argc, string argv[])
{
    // Check command-line arguments
    if (argc != 2)
    {
        printf("Usage: ./substitution key\n");
        return 1;
    }

    // key must be case insensitive

    // check key value - if not containing 26 characters or any non-alphanum char, or not containing each letter exactly once, print an error message and "return 1"

    // output planitext (without new line) and prompt user for a string of plaintext using (get_string)


    // output ciphertext (without new line) followed by ciphertext with each alpha character in plaintext substitueed for correpsonding char inc ciphertext - nonalpha should be unchanged

    // preserve case

    // print new line and exit returning 0


    printf("\n")
    return 0

}


